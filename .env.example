# Spotify App credentials (register at developer.spotify.com)
# PKCE flow is intentionally secret-free — no SPOTIFY_CLIENT_SECRET needed
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_REDIRECT_URI=https://moodify.theproductguy.cloud/auth/callback

# MCP server authentication (used by Claude to connect)
# Generate with: openssl rand -hex 32
MCP_API_KEY=generate_a_strong_random_key_here

# Server config
PORT=3000

# Database path (inside Docker volume)
DB_PATH=/data/moodify.db

# CORS origin for MCP endpoint
ALLOWED_ORIGIN=https://moodify.theproductguy.cloud
